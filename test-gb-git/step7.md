¿Qué comando uso para agregar un nuevo remoto?

***Responder en el Campus***
A. `git add remote `{{execute}}
A. `git remote add `{{execute}}
A. `git create remote `{{execute}}
B. `git new remote`{{execute}}
